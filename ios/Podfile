# Uncomment this line to define a global platform for your project
platform :ios, '14.0'

# CocoaPods analytics sends network stats synchronously affecting flutter build latency.
ENV['COCOAPODS_DISABLE_STATS'] = 'true'

project 'Runner', {
  'Debug' => :debug,
  'Profile' => :release,
  'Release' => :release,
}

def flutter_root
  generated_xcode_build_settings_path = File.expand_path(File.join('..', 'Flutter', 'Generated.xcconfig'), __FILE__)
  unless File.exist?(generated_xcode_build_settings_path)
    raise "#{generated_xcode_build_settings_path} must exist. If you're running pod install manually, make sure flutter pub get is executed first"
  end

  File.foreach(generated_xcode_build_settings_path) do |line|
    matches = line.match(/FLUTTER_ROOT\=(.*)/)
    return matches[1].strip if matches
  end
  raise "FLUTTER_ROOT not found in #{generated_xcode_build_settings_path}. Try deleting Generated.xcconfig, then run flutter pub get"
end

require File.expand_path(File.join('packages', 'flutter_tools', 'bin', 'podhelper'), flutter_root)

flutter_ios_podfile_setup

target 'Runner' do
  use_frameworks!

  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
  target 'RunnerTests' do
    inherit! :search_paths
  end
end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    # First apply Flutter's settings
    flutter_additional_ios_build_settings(target)
    
    # Then aggressively apply Firebase and pods configuration
    target.build_configurations.each do |config|
      # iOS deployment target
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '14.0'
      
      # CRITICAL: Allow non-modular headers in framework modules
      config.build_settings['CLANG_ALLOW_NON_MODULAR_INCLUDES_IN_FRAMEWORK_MODULES'] = 'YES'
      
      # Comprehensive header search paths - ensure all pods are searchable
      config.build_settings['HEADER_SEARCH_PATHS'] ||= []
      header_paths = [
        '$(inherited)',
        '$(PODS_ROOT)/Headers/Public',
        '$(PODS_ROOT)',
        '$(SDKROOT)/usr/include/libxml2'
      ]
      
      # Add all pod directories
      Dir.glob(File.expand_path('Pods/*/', File.dirname(__FILE__))) do |pod_dir|
        header_paths << pod_dir.chomp('/')
      end
      
      config.build_settings['HEADER_SEARCH_PATHS'] = header_paths.uniq
      
      # Framework search paths
      config.build_settings['FRAMEWORK_SEARCH_PATHS'] ||= []
      config.build_settings['FRAMEWORK_SEARCH_PATHS'] += [
        '$(inherited)',
        '$(PODS_ROOT)/Firebase',
        '$(PODS_ROOT)/GoogleUtilities',
        '$(PODS_ROOT)'
      ]
      config.build_settings['FRAMEWORK_SEARCH_PATHS'] = config.build_settings['FRAMEWORK_SEARCH_PATHS'].uniq
      
      # Additional settings
      config.build_settings['ENABLE_BITCODE'] = 'NO'
      config.build_settings['DEFINES_MODULE'] = 'YES'
      config.build_settings['GCC_PREPROCESSOR_DEFINITIONS'] ||= ['$(inherited)']
    end
  end
end
